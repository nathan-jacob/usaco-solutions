#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <algorithm>

int main() {
    freopen("subset.in", "r", stdin);
    //freopen("subset.out", "w", stdout);
    int n;
    vector<int> m;
    cin >> n;
    for (int i = 0; i < n; i++) {
      int temp;
      cin >> temp;
      m.push_back(temp);
    }
    
    return 0;
}
